const Post = async (request: Request) => {
  /*
   * Create a Post:
   * - Unique Identifier
   * - Creator
   * - Origin
   * - Destination
   * - Departure date/time
   * - Seats
   * - Status (open, full, cancelled, completed, expired)
   * - Comments
   * - CreatedAt
   * - UpdatedAt
   */
};

export default Post;
